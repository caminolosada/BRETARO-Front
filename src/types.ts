export interface BookStructure {
  title: string;
  author: string;
  frontPage: string;
  publicationYear: string;
  editorial: string;
  status: boolean;
  rating: number;
  destination: string;
  cosmos: string;
  id: string;
}
